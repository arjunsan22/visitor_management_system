Explanation

Nammal ippol 2 APIs undu.

GET /api/admin/dashboard

GET /api/admin/visitors

Dashboard statistics vere API.

Visitors table data vere API.

Ithu separation of concerns follow cheyyunnu.

Step 2: Thunder Client Testing
Login as Admin
POST /api/auth/login

Body

{
    "email":"admin@gmail.com",
    "password":"arjun@1234"
}

Access Token copy cheyyu.

Test 1 - Default
GET http://localhost:5000/api/admin/visitors

Headers

Authorization : Bearer <access_token>

Expected:

Visitors list
Pagination object
Test 2 - Pagination
GET http://localhost:5000/api/admin/visitors?page=1&limit=5

Expected:

{
    "statusCode":200,
    "data":{
        "data":[ ...5 visitors... ],
        "pagination":{
            "currentPage":1,
            "perPage":5,
            "totalRecords":...,
            "totalPages":...,
            "hasNextPage":...,
            "hasPreviousPage":...
        }
    },
    "message":"Visitors fetched successfully",
    "success":true
}
Test 3 - Search
GET http://localhost:5000/api/admin/visitors?search=arjun

Should return visitors whose:

name
phone
purpose

matches "arjun".

Test 4 - Department Filter
GET http://localhost:5000/api/admin/visitors?department=Computer Science

Replace "Computer Science" with exactly what is stored in your database.

Example if DB contains:

CCC

Then test:

GET /api/admin/visitors?department=CCC
Test 5 - Status Filter
GET http://localhost:5000/api/admin/visitors?status=Pending

Also test:

Verified

and

Checked Out
Test 6 - Date Filter
GET http://localhost:5000/api/admin/visitors?visit_date=2026-07-31

Use a date that actually exists in your database.

Test 7 - Combined Filters ⭐

This is the most important test.

GET /api/admin/visitors?page=1&limit=5&search=arjun&department=CCC&status=Verified&visit_date=2026-07-31

Expected:

Only visitors satisfying all selected filters should be returned.